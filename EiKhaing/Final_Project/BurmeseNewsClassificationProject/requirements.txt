# Minimal dependencies for the Streamlit UI (pin if needed)
streamlit>=1.35
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
cloudpickle>=2.2

# For the BiLSTM Keras model (CPU build)
tensorflow==2.15.*
protobuf==3.20.*

# Burmese tokenizer (optional but recommended)
pyidaungsu